# Core dependencies
torch>=2.3.0
torchvision>=0.18.0
torchaudio>=2.3.0

# Data handling
pandas>=2.0.0
numpy>=1.26.0
scikit-learn>=1.5.0

# Visualization
matplotlib>=3.8.0

# Web/app frameworks (optional for deployment)
streamlit>=1.37.0

# File & system utilities
tqdm>=4.66.0

# Optional: for safety in loading models (PyTorch 2.6+ feature)
# If your PyTorch version is 2.6 or higher, keep this note for clarity
# torch.serialization supports safe_globals in recent versions
